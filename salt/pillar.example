salt:
  master:
    version: 2019.2
    config:
      interface: 0.0.0.0
      worker_threads: 60
      file_roots:
        base:
          - /srv/salt
          - /srv/files
    repo: git@gitlab.example.com:salt-master.git # optionally git clone repo if /srv/.git does not exist
